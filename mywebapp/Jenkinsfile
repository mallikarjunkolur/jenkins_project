pipeline { 
    agent any 
    stages { 
        stage('clean') { 
            steps { 
                sh 'cd mywebapp && mvn clean' 
            } 
        } 
        stage('compile') { 
            steps { 
                sh 'cd mywebapp && mvn compile' 
            } 
        } 
        stage('test') { 
            steps { 
                sh 'cd mywebapp && mvn test' 
            } 
        } 
    } 
} 

